import pyjokes

joke = pyjokes.get_joke('en', 'neutral')

print(joke)

#usare pip per vedere i pacchetti installati
#per installare packages --> pip3 install pyjokes
#Se no, non carica perchè il pacchetto può essere installato solo su pycharme o altro ma nn nel sistema
#pip list mostra la lista dei pacchetti
#pip -V indica la versione